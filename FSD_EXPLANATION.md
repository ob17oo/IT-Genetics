# Правильное понимание FSD + Next.js

## ❌ Неправильное понимание:

```
src/app/pages/          ← Такой папки НЕТ в Next.js App Router
  └── page.tsx
src/pages/              ← Компоненты для app/pages/
  └── home-page.tsx
```

## ✅ Правильное понимание:

### Структура Next.js App Router:

```
src/app/                    # Роутинг Next.js (технический слой)
├── (home)/                 # Route group → маршрут: /
│   └── page.tsx           # Next.js страница (роут)
├── about/                 # → маршрут: /about
│   └── page.tsx
└── layout.tsx            # Общий layout
```

### Структура FSD:

```
src/pages/                 # FSD слой: композиция страниц (бизнес-логика)
├── home/                  # Страница "Главная"
│   └── ui/
│       └── home-page.tsx # Компонент страницы
└── about/
    └── ui/
        └── about-page.tsx
```

---

## 🔗 Как они связаны:

### Пример 1: Главная страница

**1. Next.js роут** (`src/app/(home)/page.tsx`):
```tsx
// Это технический слой - определяет маршрут "/"
import { HomePage } from "@/pages/home/ui/home-page"

export default function Home() {
  return <HomePage />  // Импортирует из FSD слоя pages/
}
```

**2. FSD компонент страницы** (`src/pages/home/ui/home-page.tsx`):
```tsx
// Это бизнес-слой - композиция фич
import { OfficeScene } from "@/entities/scene/ui/office-scene"
import { GameControls } from "@/features/game-controls/ui/controls"

export function HomePage() {
  return (
    <section>
      <OfficeScene />
      <GameControls />
    </section>
  )
}
```

### Пример 2: Страница "О нас"

**1. Next.js роут** (`src/app/about/page.tsx`):
```tsx
// Маршрут: /about
import { AboutPage } from "@/pages/about/ui/about-page"

export default function About() {
  return <AboutPage />
}
```

**2. FSD компонент** (`src/pages/about/ui/about-page.tsx`):
```tsx
export function AboutPage() {
  return <div>О нас</div>
}
```

---

## 📊 Сравнение:

| Аспект | `src/app/` | `src/pages/` |
|--------|-----------|--------------|
| **Назначение** | Роутинг Next.js | Композиция страниц FSD |
| **Что определяет** | URL маршруты | UI и бизнес-логику страниц |
| **Содержит** | `page.tsx`, `layout.tsx`, `route.ts` | Компоненты страниц |
| **Импортирует** | Из `pages/` | Из `widgets/`, `features/`, `entities/` |
| **Технический/Бизнес** | Технический слой | Бизнес-слой |

---

## 🎯 Правило:

**`src/app/`** = **"Где показывать"** (роутинг)  
**`src/pages/`** = **"Что показывать"** (композиция)

---

## 💡 Аналогия:

Представьте ресторан:

- **`app/`** = Столы в зале (где сидят гости) - это роутинг
- **`pages/`** = Меню и блюда (что подают) - это композиция

Столы (роуты) определяют, где гости сидят, но не определяют, что им подавать.  
Меню (pages) определяет, что подавать, но не определяет, где гости сидят.

---

## ✅ Итог:

- **`src/app/`** - это НЕ папка `app/pages/`, а корневая папка роутинга
- **`src/pages/`** - это НЕ компоненты для `app/pages/`, а FSD слой композиции
- **`app/(home)/page.tsx`** импортирует **`pages/home/ui/home-page.tsx`**
- Это разные слои с разными ответственностями

