export interface Achievment {
    id: number,
    title: string,
    description: string,
    icon: string,
    reward: number,
    unlocked: boolean,
    category: string,
    rarity: string,
    progress: number,
}